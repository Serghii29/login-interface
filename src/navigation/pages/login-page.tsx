/* eslint-disable jsx-a11y/label-has-associated-control */
import React from 'react';
import { Login } from '../../components/login';

const LoginPage: React.FC = React.memo(() => <Login />);

export default LoginPage;
