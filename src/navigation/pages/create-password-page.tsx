import React from 'react';
import { CreateNewPassword } from '../../components/create-new-password';

const CreatePasswordPage: React.FC = React.memo(() => <CreateNewPassword />);

export default CreatePasswordPage;
